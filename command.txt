git config --global user.name "Sonal"
git config --global user.email "sonalvarg@gmail.com"

git init => for initializing git in pwd

git status => list all untracked files

git add -A => adding all changes to staging area

git commit -m "commit message" => adding stated files to local repository

git remote add origin https://github.com/SeanEmM/gitbasic.git

git remote -v


following update was done in remote repository
git branch

git branch student

git checkout student 

git checkout master

git merge student
